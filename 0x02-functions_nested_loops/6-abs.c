#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int value to calling function
 */

int _abs(int n)

{
	if (n >= 0)

	{
		return (n);
	}

	return (-n);

}
